{
  "collectionName": "components_product_pricing_tiers",
  "info": {
    "displayName": "Pricing Tier",
    "icon": "shopping-cart",
    "description": "Commercial pricing package (e.g. Starter, Professional, Enterprise)"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Plan name (e.g. Starter School, Growth Automation, Enterprise Network)"
    },
    "price": {
      "type": "string",
      "required": true,
      "description": "Display price text (e.g. $299, $699, Custom)"
    },
    "billingPeriod": {
      "type": "enumeration",
      "enum": [
        "monthly",
        "yearly",
        "custom"
      ],
      "default": "monthly",
      "description": "Billing recurrence cycle"
    },
    "tagline": {
      "type": "string",
      "description": "Target audience summary (e.g. Ideal for single campuses up to 500 students)"
    },
    "badge": {
      "type": "string",
      "description": "Highlighted badge (e.g. Most Popular Choice, Single Campus, Multi-Branch Group)"
    },
    "description": {
      "type": "text",
      "description": "Additional plan details or SLA commitments"
    },
    "features": {
      "type": "component",
      "repeatable": true,
      "component": "product.pricing-feature",
      "description": "Checklist of included entitlements"
    },
    "isFeatured": {
      "type": "boolean",
      "default": false,
      "description": "Visually elevate this tier with highlight borders and glow styling"
    },
    "ctaLabel": {
      "type": "string",
      "default": "Request Demo & Quote",
      "description": "Text displayed on the pricing button"
    },
    "ctaLink": {
      "type": "string",
      "default": "/contact",
      "description": "Target link or booking action"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "Display sequence order"
    }
  }
}
